<%= form_with(model: prerequisite) do |form| %>
  <% if prerequisite.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(prerequisite.errors.count, "error") %> prohibited this prerequisite from being saved:</h2>

      <ul>
        <% prerequisite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :required_project_id, style: "display: block" %>
    <%= form.text_field :required_project_id %>
  </div>

  <div>
    <%= form.label :locked_project_id, style: "display: block" %>
    <%= form.text_field :locked_project_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
