<%= form_with(model: attachment) do |form| %>
  <% if attachment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(attachment.errors.count, "error") %> prohibited this attachment from being saved:</h2>

      <ul>
        <% attachment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :file_extension, style: "display: block" %>
    <%= form.text_field :file_extension %>
  </div>

  <div>
    <%= form.label :url, style: "display: block" %>
    <%= form.text_field :url %>
  </div>

  <div>
    <%= form.label :uploader_id, style: "display: block" %>
    <%= form.text_field :uploader_id %>
  </div>

  <div>
    <%= form.label :attachable_id, style: "display: block" %>
    <%= form.text_field :attachable_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
