<%= form_with(model: ownership) do |form| %>
  <% if ownership.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ownership.errors.count, "error") %> prohibited this ownership from being saved:</h2>

      <ul>
        <% ownership.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :owner_id, style: "display: block" %>
    <%= form.text_field :owner_id %>
  </div>

  <div>
    <%= form.label :house_id, style: "display: block" %>
    <%= form.text_field :house_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
